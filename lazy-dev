#/bin/bash
docker run -it --rm \
    -v "$(pwd):/app" \
    -v "/var/run/docker.sock:/var/run/docker.sock" \
    -p "16827:80" \
    -w /app \
    ierceg/node-dev:6.9.1 \
    nodemon -V -d 1 -L -w /app index.js
